// Consumers copy this file next to `lib/` (`cp core/profile.js`), so it has to exist; the
// reference profile itself lives in lib/reference-profile.ts and nothing imports this shim.
export * from "./lib/reference-profile.ts"
