---
title: "The toolkit, as a folder"
type: concept
description: "A folder note: the file is named after the folder it opens, so the site serves it as that folder's index and not at its authored path."
tags: [fixture]
aliases: [toolkit]
---

A renderer collapses this note into `/toolkit/`. The graph must publish that url, or the
dock and `Open ↗` would open the authored path, which no site ever writes.

# Topology

* **About**: [[tools/okf]]
