node_modules/
package-lock.json
*.log

# Build output of the plugins: every one compiles with tsup at install time.
plugins/*/dist/

# The site the smoke build writes: rebuilt from the fixture on every run.
harness/fixture/public/
